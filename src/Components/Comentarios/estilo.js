import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
    comentarios: {
        flexDirection: "row",
        alignItems: "center",
        marginLeft: 5
    }
})

export default estilo